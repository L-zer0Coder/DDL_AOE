## 2026-09-10 祭司探索策略定稿(仅讨论/给代码, 未改文件)
- 用户确认的四阶段方案: phase0 探自己所在角(以市镇中心为界取半图) → phase1 绕地图中心(50,50)半径42→6步长8收圈 → phase2 探另外三个角(锚点15/85) → done。
- 新增危险规避: dangerList = 视野内 enemy_armies + 活着(Blood>0)的狮子/大象; 选点用 pathSafe(点到线段距离) 过滤, 走路中进入危险半径则 8 格背离改道(冷却60帧)。
- 关键约束: 目的地必须是 Open(已探明陆地), 绝不把 Unknown/Ocean/资源/建筑格当目标; 超时250帧/到达后格子冷却400帧防原地踏步。
- 交付方式: 用户要求"你先写了给我看, 我自己誊抄", 且明确不得直接改文件夹内文件。
## 2026-09-10 祭司卡住 + 房子不建 定位(读日志)
- 祭司: 日志显示同一坐标(块7,5)每4ms重复 HumanMove 数分钟 → 无 IDLE 判断导致每帧重发同一目标, 移动被反复重置; 且扫描顺序固定, 目标永远是框左上角, 到达后也不会换点。
- 建房: gethomeBuilder() 第328行写成 homeBlockUR=f.BlockUR(应为 homeBuilderUR), 把房屋选址基准覆盖成村民坐标 → 算出的选址是乱点 → "位置非空地"建造失败。
- 另发现: cnt_build++%8 在同一帧用了两次, dr/ur 偏移索引错位; HumanBuild 前未判断是否已走到目标(与上一行 HumanMove 打架)。
## 2026-09-10 祭司探路重写(已落盘, 仅改 priestExplore)
- 用户授权修改, 但限定只动 priestExplore()。实现: 阶段0探自己角(半图) → 阶段1绕中心(50,50)半径40→8步长8的环带找边界点 → 结束(不探另外三角)。
- 避障两层: 选点前 dangerNear 过滤(敌兵/敌农/活狮子大象 8格内不选) + 每帧检测危险则8方向背离撤退6格(冷却80帧)。
- 防卡死: 300帧超时拉黑该格1500帧; 边界点判定(Open且5x5有Unknown)保证到达后自动换点; 走路中不打断。
- 改动范围校验: 与原文件 diff 只有 priestExplore 区域; 备份在 .workbuddy/UsrAI_backup_20260910.cpp。
- 已知覆盖缺口: 环带只覆盖切比雪夫距离<=41的区域, 距中心>41的外圈不会被探到; 需要全图覆盖可把初始 radius 改 48。用户已明确: 不负责编译, 只写代码。

## 2026-09-10 建造调度重写(manageBuild, 已落盘)
- 需求: 优化建房 + "砍树/猎瞪羚前先看周围有没有仓库, 没有就先建仓库"。授权不限范围, 但要求事后报告改点。
- 新增全局: `unordered_map<int,ResRec>gathering`(SN->采集资源类型/块坐标), `unordered_map<int,int>badBuildSpot`(失败选址->冷却帧), `bool buildBusy`(建造工本轮是否有活)。
- processData(): 删除原内联建房块(含 cnt_build 双自增 bug), 改调 `manageBuild()`; 两个采集循环在派活时记录 `gathering[r.SN]`。
- getBaseInfo(): 删除开局盲目 `HumanBuild(STOCK, gazelle-3)`。
- 新函数(追加在 gethomeBuilder 之后): `hasBuilding/hasUnfinishedBuilding/nearFinishedBuilding/findBuildSpot/manageBuild`。
- manageBuild 优先级: T1房屋(人口余量<=2, 每房+4) → T2谷仓(正在采浆果且10格内无谷仓/中心) → T3仓库(采树/石/金/瞪羚/大象/狮子且10格内无仓库/中心) → T4兵营 → T5市场(需谷仓+工具时代) → T6靶场(需兵营+工具时代) → T7马厩(需兵营+工具时代)。
- 落点校验 findBuildSpot: size×size 全为 Open(说明无资源无建筑) + 高度一致且非负(非斜坡/海) + 未被拉黑; size 房屋=2、其余=3(对应 SIZELEN_SMALL/MIDDLE)。
- 已核对引擎语义: `HumanBuild(SN,建筑号,BlockDR,BlockUR)` 的 BlockDR/UR 是**左下角锚点**, 占地 = BlockDR+size-1(Core_List.cpp:730 is_BuildingCanBuild)。
- 防同帧打架: buildBusy=true 时采集循环不再给建造工派采集, 否则 manageBuild 的"走过去"会被随后 HumanAction 覆盖, 导致永远不建房。
- 防时代卡死: 市场/靶场/马厩 gate 上 `info.civilizationStage>=CIVILIZATION_TOOLAGE`(config.h:301), 否则兵营在石器时代先成会去硬建靶场必然失败→反复拉黑换点。

